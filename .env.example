# Database Configuration - Neon PostgreSQL (https://neon.tech)
# Replace with your Neon database connection string
DATABASE_URL="postgresql://[username]:[password]@[neon-host]/[database]?sslmode=require"

# Authentication (NextAuth.js)
# Generate a random string for NEXTAUTH_SECRET: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# Optional: Only needed if deploying to production
# NEXTAUTH_URL="https://your-production-url.com"

# Application Settings
NODE_ENV="development"